{
  "id": "python-custom-exceptions",
  "slug": "custom-exceptions",
  "title": "Raising and Custom Exceptions",
  "description": "Trigger exceptions with raise and create domain-specific custom exception classes.",
  "pathId": "python",
  "moduleId": "python-reliable-programs",
  "difficulty": "medium",
  "estimatedMinutes": 10,
  "concepts": [
    "raise",
    "custom-exceptions",
    "user-defined-exceptions",
    "exception-hierarchy"
  ],
  "blocks": [
    {
      "type": "text",
      "content": "A missing configuration value needs a clear signal that callers can handle. You can create an error that names the problem. What could you ask Python to do here?"
    },
    {
      "type": "text",
      "content": "## Raising Exceptions with `raise`\n\nYou can manually trigger any exception with the `raise` keyword:\n\n```python\ndef set_percentage(value):\n    if not 0 <= value <= 100:\n        raise ValueError(f\"Percentage must be between 0 and 100, got {value}\")\n    return value\n```"
    },
    {
      "type": "multipleChoice",
      "question": "What is the recommended base class when creating user-defined exceptions in Python?",
      "options": [
        "Exception",
        "BaseException",
        "object",
        "RuntimeWarning"
      ],
      "correctIndex": 0,
      "explanation": "Custom exceptions should almost always inherit from `Exception` (not `BaseException`, which is reserved for system-exiting exceptions like `KeyboardInterrupt` and `SystemExit`)."
    },
    {
      "type": "code",
      "language": "python",
      "starterCode": "class InvalidPasswordError(Exception):\n    pass\n\ndef validate_password(password):\n    if len(password) < 8:\n        raise InvalidPasswordError(\"Password must be at least 8 characters long\")\n    return True\n\ntry:\n    validate_password(\"short\")\nexcept InvalidPasswordError as err:\n    print(f\"Validation failed: {err}\")\n"
    },
    {
      "type": "challenge",
      "language": "python",
      "prompt": "Write a function `process_transaction(balance, amount)` that checks if `amount > balance`. If it is, raise a custom exception `NegativeBalanceError` with the message `'Insufficient funds'`. Catch it within the function and return `'Declined'`. If `amount <= balance`, return `'Approved'`.",
      "starterCode": "class NegativeBalanceError(Exception):\n    pass\n\ndef process_transaction(balance, amount):\n    # your code here\n    pass\n",
      "tests": [
        {
          "input": [
            100,
            50
          ],
          "expectedOutput": "Approved",
          "description": "returns 'Approved' when funds are sufficient"
        },
        {
          "input": [
            50,
            100
          ],
          "expectedOutput": "Declined",
          "description": "returns 'Declined' when amount > balance triggers custom exception"
        },
        {
          "input": [
            200,
            200
          ],
          "expectedOutput": "Approved",
          "description": "returns 'Approved' when exact balance is withdrawn"
        }
      ]
    }
  ]
}
